# Arduino / build output
build/
*.hex
*.bin
*.elf

# PlatformIO (if used later)
.pio/

# WiFi / backend credentials — never commit real secrets
# (any secrets.h under firmware/, e.g. src/sign-on/ and src/telemetry/)
firmware/**/secrets.h

# Editors
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db
